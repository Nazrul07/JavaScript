// A class is a BLUEPRINT — like a template
// We define it once, create many objects from it

class Person {
    // everything about a Person goes here
}

// Now create actual people from the blueprint
const p1 = new Person()
const p2 = new Person()
const p3 = new Person()

// Think of a class like a cookie cutter — the cutter is the class, the cookies are the objects.